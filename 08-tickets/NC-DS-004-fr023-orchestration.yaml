ticket_id: NC-DS-004
ticket_type: FEATURE
status: DONE
priority: MEDIUM
assigned_to: deepseek-v4-flash
created_at: '2026-04-12T04:19:00'
created_by: antigravity-t0
context:
  roadmap_ticket: FR-023
  active_locks:
  - server.py
  - sub_server.py
  - NC-NAM-FR-001
  - neocortex_config.yaml
  write_zone: 01_neocortex_framework/neocortex/mcp/tools/
  naming_rule: NC-<TIPO>-<SIGLA>-<NUM>-<desc>.ext
  reference_file: neocortex/mcp/tools/NC-TOOL-FR-029-health.py
  warning: FR-023 NÃO toca sub_server.py — apenas orquestra via HTTP. Se precisar
    tocar sub_server.py → ESCALATE status=ESCALATED imediatamente
task:
  description: 'Criar NC-TOOL-FR-023-orchestration.py com register_tool(mcp) e ações:


    agent.spawn       → inicia sub-servidor efêmero via neocortex_subserver (HTTP,
    não stdio)

    agent.heartbeat   → verifica status de agente em execução

    agent.consume     → consome resultado de agente efêmero

    agent.list_ephemeral → lista agentes efêmeros ativos

    task.execute      → envia task para sub-servidor via HTTP POST /task

    task.get_result   → recupera resultado de task executada

    peers.discover    → descobre instâncias NeoCortex disponíveis na rede local


    Absorve: NC-TOOL-FR-015-subserver.py + NC-TOOL-FR-018-task.py (partes de orquestração)

    ATENÇÃO: A comunicação é via HTTP (porta 8766+). NÃO modificar sub_server.py.

    Padrão: seguir estrutura de NC-TOOL-FR-029-health.py

    '
  entry_state:
    files_exist:
    - neocortex/mcp/tools/NC-TOOL-FR-015-subserver.py
    - neocortex/mcp/tools/NC-TOOL-FR-018-task.py
    files_must_not_exist:
    - neocortex/mcp/tools/NC-TOOL-FR-023-orchestration.py
  exit_state:
    files_created:
    - neocortex/mcp/tools/NC-TOOL-FR-023-orchestration.py
    files_modified: null
    ssot_updated: true
    compiles_ok: true
    roadmap_done: FR-023
barriers:
- id: B1
  check: naming_convention
  rule: NC-TOOL-FR-023-orchestration.py
  block_if_fail: true
- id: B2
  check: lock_guard
  rule: NUNCA modificar sub_server.py — ESCALATE se necessário
  block_if_fail: true
- id: B3
  check: compile
  rule: py_compile OK
  block_if_fail: true
- id: B4
  check: ssot_entry
  rule: NC-NAM-FR-001 atualizado [2026-04-12]
  block_if_fail: true
- id: B5
  check: no_hardcode
  rule: Usar get_config()
  block_if_fail: false
- id: B6
  check: no_print
  rule: Usar logger
  block_if_fail: false
limits:
  max_validation_rounds: 5
  escalate_to_t0_after: 3
  max_tokens_per_phase: 4000
integrity_hash: 3770dfa24d412371
protocol:
  rca_applied: true
  three_w_applied: true
  kiss_check: <2500 chars per ticket description
  validation:
    py:
    - ruff check --fix
    - ruff format
    - ruff check
    - mypy --ignore-missing-imports
    - py_compile
    - bandit -q
    yaml:
    - yaml.safe_load
    - naming NC- check
    - secret scan
    mdc:
    - frontmatter YAML validate
    - NC-READ-HASH check
    - naming NC- check
    json:
    - json.loads parse
    - naming NC- check
    all:
    - '@LOCKS check'
    - Write Zone check
    - '@SSOT update'
    - Handoff YAML
    - 3W check
    - SSOT Header
  sandbox_required: true
  sandbox_steps:
  - 1. Copiar arquivo alvo para *-sandbox.py
  - 2. Fazer alteracoes no sandbox
  - 3. Validar sandbox (ruff+mypy+py_compile)
  - 4. Testar sandbox
  - 5. Aplicar no real (com backup)
  - 6. Rollback via backup se falhar
  handoff_required: true
  handoff_template: DIR-DS-001-tickets/NC-DS-HANDOFF-TEMPLATE.yaml
  handoff_dest: DIR-DS-002-audit-logs/
  handoff_format: NC-DS-{TICKET_ID}-handoff-{YYYYMMDDTHHMMSS}.yaml
  handoff_fields:
  - ticket_id
  - status
  - submitted_at
  - submitted_by
  - summary
  - files_created
  - files_modified
  - validation_rounds
  - barriers_passed
  - locks_violated
  - t0_review
  governance:
    eisenhower_applied: true
    mordaca_h: HOOK — bloqueia acao invalida ANTES de executar (FAIL-FAST)
    mordaca_c: CHECKPOINT — audita estado a cada 300s (PulseScheduler)
    mordaca_s: SCHEDULE — consolida no CICLO 4 semanal
    mordaca_u: USER — T0 aprova/rejeita via handoff review
    kaizen: Registrar micro-melhoria no @CHANGELOG apos conclusao
    roadmap: Marcar %DONE no @ROADMAP apos handoff aprovado
