# @UBL @UBL @DS-045 | LEXICO: #TICKETS
id: NC-DS-045
code: HUD-LAUNCHERS
status: DONE
title: HUD Launchers (Mission Control & Pixel Agents) e Webhooks
assignee: Agente OpenCode na porta 51148
context:
- 02_memory_lobes/NC-LBE-INT-004-mission-control.mdc
- 02_memory_lobes/NC-LBE-INT-005-pixel-agents.mdc
- 01_neocortex_framework/neocortex_hud.py
task: '**OBJETIVO**: Alterar a interface principal em `neocortex_hud.py` para disponibilizar
  atalhos de acompanhamento visual tanto para Kanban (Mission Control) quanto Observabilidade
  gráfica (Pixel Agents).


  **O QUE FAZER**:

  1. Leia `neocortex_hud.py` inteiro sem pressa para entender onde injetar as novas
  features.

  2. Crie uma nova aba/painel "Cockpit Integrado" ou insira botões de Launch bem nítidos.

  3. Crie a função "Abrir Dashboard Mission Control" que desponta via biblioteca `webbrowser`
  ou subproc um acesso para http://localhost:3000.

  4. Crie a função "Pixel Agents VS Code" que executa comando similar a `subprocess.Popen(["code",
  "<caminho_para_pasta_turbov42>"])` conforme documentado no Lobe 5.

  5. Além disso, leia os specs do Mission Control sobre o ''Webhook Bridge'' (integração
  do HookRegistry que manda pro webhooks/inactive do MissionCtrl) e estruture uma
  chamada ou rascunho de como seria esse Hook em `01_neocortex_framework/neocortex/core/hooks/NC-HK-FR-002-missionctrl.py`.


  **ATENÇÃO R21**: A prioridade aqui é não "quebrar o Tkinter/CustomTkinter" existente.
  Teste que o código sintaticamente processa e faz launch.

  '
protocol:
  rca_applied: true
  three_w_applied: true
  kiss_check: <2500 chars per ticket description
  validation:
    py:
    - ruff check --fix
    - ruff format
    - ruff check
    - mypy --ignore-missing-imports
    - py_compile
    - bandit -q
    yaml:
    - yaml.safe_load
    - naming NC- check
    - secret scan
    mdc:
    - frontmatter YAML validate
    - NC-READ-HASH check
    - naming NC- check
    json:
    - json.loads parse
    - naming NC- check
    all:
    - '@LOCKS check'
    - Write Zone check
    - '@SSOT update'
    - Handoff YAML
    - 3W check
    - SSOT Header
  sandbox_required: true
  sandbox_steps:
  - 1. Copiar arquivo alvo para *-sandbox.py
  - 2. Fazer alteracoes no sandbox
  - 3. Validar sandbox (ruff+mypy+py_compile)
  - 4. Testar sandbox
  - 5. Aplicar no real (com backup)
  - 6. Rollback via backup se falhar
  handoff_required: true
  handoff_template: DIR-DS-001-tickets/NC-DS-HANDOFF-TEMPLATE.yaml
  handoff_dest: DIR-DS-002-audit-logs/
  handoff_format: NC-DS-{TICKET_ID}-handoff-{YYYYMMDDTHHMMSS}.yaml
  handoff_fields:
  - ticket_id
  - status
  - submitted_at
  - submitted_by
  - summary
  - files_created
  - files_modified
  - validation_rounds
  - barriers_passed
  - locks_violated
  - t0_review
  governance:
    eisenhower_applied: true
    mordaca_h: HOOK — bloqueia acao invalida ANTES de executar (FAIL-FAST)
    mordaca_c: CHECKPOINT — audita estado a cada 300s (PulseScheduler)
    mordaca_s: SCHEDULE — consolida no CICLO 4 semanal
    mordaca_u: USER — T0 aprova/rejeita via handoff review
    kaizen: Registrar micro-melhoria no @CHANGELOG apos conclusao
    roadmap: Marcar %DONE no @ROADMAP apos handoff aprovado
