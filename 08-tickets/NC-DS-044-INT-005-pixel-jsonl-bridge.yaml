# @UBL @UBL @DS-044 | LEXICO: #TICKETS
id: NC-DS-044
code: INT-002 e INT-003
status: DONE
title: Bridge JSONL do Pixel Agents
assignee: Agente OpenCode na porta 47730
context:
- 02_memory_lobes/NC-LBE-INT-005-pixel-agents.mdc
- 01_neocortex_framework/NC-HK-FR-001-hook-registry.py
task: '**OBJETIVO**: Desenvolver a ponte JSONL interceptando os hooks do NeoCortex
  para animar os robôs do Pixel Agents dentro do VS Code.


  **O QUE FAZER**:

  1. Leia os guias internos presentes em `NC-LBE-INT-005-pixel-agents.mdc` detalhadamente.

  2. A documentação contém a especificação completa de como a bridge do HookRegistry
  emite dados num formato compatível com o "Claude Code JSONL".

  3. Construa o script em `01_neocortex_framework/scripts/NC-SCR-FR-016-opencode-jsonl-bridge.py`.
  A estrutura base está completamente exposta na documentação do lobe!

  4. Garanta que ele gere diretórios em `~/.claude/projects/neocortex-agents/{agent_id}`
  se assegurando de criar os parents com expanduser.

  5. Mantenha os formats de Data em formato ISO com a letra "Z" no final para assegurar
  que a UI do WebView do Pixel Agents leia e não congele.


  **ATENÇÃO MASTER RULE R21**: Verifique imports e se a estrutura da classe base HookRegistry
  não vai quebrar `get_hook_registry()`.

  '
protocol:
  rca_applied: true
  three_w_applied: true
  kiss_check: <2500 chars per ticket description
  validation:
    py:
    - ruff check --fix
    - ruff format
    - ruff check
    - mypy --ignore-missing-imports
    - py_compile
    - bandit -q
    yaml:
    - yaml.safe_load
    - naming NC- check
    - secret scan
    mdc:
    - frontmatter YAML validate
    - NC-READ-HASH check
    - naming NC- check
    json:
    - json.loads parse
    - naming NC- check
    all:
    - '@LOCKS check'
    - Write Zone check
    - '@SSOT update'
    - Handoff YAML
    - 3W check
    - SSOT Header
  sandbox_required: true
  sandbox_steps:
  - 1. Copiar arquivo alvo para *-sandbox.py
  - 2. Fazer alteracoes no sandbox
  - 3. Validar sandbox (ruff+mypy+py_compile)
  - 4. Testar sandbox
  - 5. Aplicar no real (com backup)
  - 6. Rollback via backup se falhar
  handoff_required: true
  handoff_template: DIR-DS-001-tickets/NC-DS-HANDOFF-TEMPLATE.yaml
  handoff_dest: DIR-DS-002-audit-logs/
  handoff_format: NC-DS-{TICKET_ID}-handoff-{YYYYMMDDTHHMMSS}.yaml
  handoff_fields:
  - ticket_id
  - status
  - submitted_at
  - submitted_by
  - summary
  - files_created
  - files_modified
  - validation_rounds
  - barriers_passed
  - locks_violated
  - t0_review
  governance:
    eisenhower_applied: true
    mordaca_h: HOOK — bloqueia acao invalida ANTES de executar (FAIL-FAST)
    mordaca_c: CHECKPOINT — audita estado a cada 300s (PulseScheduler)
    mordaca_s: SCHEDULE — consolida no CICLO 4 semanal
    mordaca_u: USER — T0 aprova/rejeita via handoff review
    kaizen: Registrar micro-melhoria no @CHANGELOG apos conclusao
    roadmap: Marcar %DONE no @ROADMAP apos handoff aprovado
