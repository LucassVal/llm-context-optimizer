# NC-CFG-FR-006 — NeoCortex Framework Dependencies
# Updated: 2026-04-30 | T0 Antigravity | NC-DS-223
# Ground truth verified via STEP 0 environment check

# Protocol dependencies
mcp>=1.27.0           # Model Context Protocol server implementation
fastmcp>=3.2.0        # FastMCP server framework

# Data & serialization
ruamel.yaml>=0.18.0   # YAML parser with round-trip support
PyYAML>=6.0.0         # YAML fallback parser
msgspec>=0.21.0       # High-performance JSON serialization
jsonschema>=4.26.0    # JSON schema validation

# Storage & caching
diskcache>=5.6.0      # Persistent disk caching
cachetools>=6.2.0     # In-memory caching utilities
duckdb>=1.5.0         # Analytical SQL database

# System & utilities
psutil>=7.2.0         # System monitoring
platformdirs>=4.5.0   # Platform-specific directory paths
rich>=14.2.0          # Terminal formatting

# Security & cryptography
cryptography>=42.0.0  # Fernet (AES-128-CBC + HMAC-SHA256)

# Notifications
notifypy>=0.3.0       # Desktop notifications

# HTTP & async
aiohttp>=3.13.0       # Async HTTP client/server

# Dev dependencies (install separately via pip install -r requirements-dev.txt)
# ruff>=0.15.0        # Linting
# mypy>=1.20.0        # Type checking
# pytest>=9.0.0       # Testing
# bandit>=1.9.0       # Security scanning
