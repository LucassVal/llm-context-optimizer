ticket_id: NC-DS-255
status: OPEN
priority: HIGH
title: Semantic Response Cache com Embedding Similarity — eliminar chamadas a API
front: 'CICLO 5: API EFICIENCIA'
description: 'RCA: Infraestrutura existe (KG + LanceDB + Semantic Router) mas cache
  ainda e visto como hash exato, nao similaridade semantica. 3W: WHAT=Indexar respostas
  da API no LanceDB com embedding. Novo prompt: similaridade >0.92 → retorna cache
  sem chamar API. WHY=Elimina completamente a chamada. 15-25% de reuso economiza esse
  percentual do custo total. WHERE=Semantic Router (FR-165) + KG. KISS: <300 linhas.
  Usar infra existente. VALIDACAO: ruff+mypy+py_compile. Teste: prompts similares
  → verificar cache hit. SANDBOX: Testar com log de prompts. HANDOFF: YAML com taxa
  de cache hit. ESFORCO: Alto. IMPACTO: Eliminacao de chamadas API. DEPENDENCIAS:
  Nenhuma (infra existe).'
created_at: '2026-04-30T14:50:41'
created_by: T0-Antigravity
roadmap_ref: CICLO_5
protocol:
  rca_applied: true
  three_w_applied: true
  kiss_check: true
  validation:
  - ruff
  - mypy
  - py_compile
  - bandit
  sandbox_required: true
  handoff_required: true
  handoff_template: DIR-DS-001-tickets/NC-DS-HANDOFF-TEMPLATE.yaml
  handoff_dest: DIR-DS-002-audit-logs/
