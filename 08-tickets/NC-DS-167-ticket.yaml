agent: T0
closed_at: '2026-04-28T23:24:34.594746'
created: '2026-04-22T09:44:13'
description: 'Consolidar arquivos de regras de governança:

  1. SSOT atual: DIR-DOC-FR-001-docs-main/NC-GOV-FR-003-ia-governance-rules.yaml (23
  regras)

  2. Novo arquivo: governance/rules/NC-GOV-RULES-v0.3-20260422.yaml (30 regras)

  3. Auditor NC-SCR-FR-080 usa 20 regras definidas em código


  Problemas:

  - Regras R05-R06, R11, R14-R17 existem no SSOT mas não no novo arquivo

  - Compliance score fictício (100% vs 19.7% real)

  - Status NEW_ACTIVE não compatível com auditor

  - Duplicação de arquivos causa confusão


  Ações:

  1. Atualizar SSOT com novas regras R22-R30

  2. Manter compatibilidade com auditor

  3. Corrigir numeração sequencial

  4. Ajustar compliance score para realidade

  5. Definir arquivo canônico único

  '
priority: HIGH
resolution: 'RESOLVED: YAML 94.5% (52/55). 3 legacy em dirs orfaos sem impacto.'
status: DONE
ticket_id: NC-DS-167
title: Consolidação SSOT Regras de Governança
protocol:
  rca_applied: true
  three_w_applied: true
  kiss_check: <2500 chars per ticket description
  validation:
    py:
    - ruff check --fix
    - ruff format
    - ruff check
    - mypy --ignore-missing-imports
    - py_compile
    - bandit -q
    yaml:
    - yaml.safe_load
    - naming NC- check
    - secret scan
    mdc:
    - frontmatter YAML validate
    - NC-READ-HASH check
    - naming NC- check
    json:
    - json.loads parse
    - naming NC- check
    all:
    - '@LOCKS check'
    - Write Zone check
    - '@SSOT update'
    - Handoff YAML
    - 3W check
    - SSOT Header
  sandbox_required: true
  sandbox_steps:
  - 1. Copiar arquivo alvo para *-sandbox.py
  - 2. Fazer alteracoes no sandbox
  - 3. Validar sandbox (ruff+mypy+py_compile)
  - 4. Testar sandbox
  - 5. Aplicar no real (com backup)
  - 6. Rollback via backup se falhar
  handoff_required: true
  handoff_template: DIR-DS-001-tickets/NC-DS-HANDOFF-TEMPLATE.yaml
  handoff_dest: DIR-DS-002-audit-logs/
  handoff_format: NC-DS-{TICKET_ID}-handoff-{YYYYMMDDTHHMMSS}.yaml
  handoff_fields:
  - ticket_id
  - status
  - submitted_at
  - submitted_by
  - summary
  - files_created
  - files_modified
  - validation_rounds
  - barriers_passed
  - locks_violated
  - t0_review
  governance:
    eisenhower_applied: true
    mordaca_h: HOOK — bloqueia acao invalida ANTES de executar (FAIL-FAST)
    mordaca_c: CHECKPOINT — audita estado a cada 300s (PulseScheduler)
    mordaca_s: SCHEDULE — consolida no CICLO 4 semanal
    mordaca_u: USER — T0 aprova/rejeita via handoff review
    kaizen: Registrar micro-melhoria no @CHANGELOG apos conclusao
    roadmap: Marcar %DONE no @ROADMAP apos handoff aprovado
