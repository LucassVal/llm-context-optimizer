---

<!-- NC-READ-HASH: NC-LBE-FR-NEURAL-MAP-001-v1 -->
<!-- DEDUP: Se NC-READ-HASH: NC-LBE-FR-NEURAL-MAP-001-v1 ja esta no contexto desta sessao, SALTE. -->
lobe_id: NC-LBE-FR-NEURAL-MAP-001
domain: 06_governance
type: ARCHITECTURE_MAP
created: 2026-04-29
status: ACTIVE
format: MDC (OpenClaude native)
temperature: 0
status: archived
---

@UBL $NEURAL_MAP
LEXICO: #neural #map #hierarchy #interdependencias #architecture


<!-- NC-READ-HASH: NC-LBE-FR-NEURAL-MAP-001-v1 -->
<!-- DEDUP: Se NC-READ-HASH: NC-LBE-FR-NEURAL-MAP-001-v1 ja esta no contexto desta sessao, SALTE. -->

@UBL NeuralMap
LEXICO: #neural #map #hierarquia #T0 #T1 #T2

# 1. NeoCortex Neural Map — Hierarquia + Interdependências

@UBL T0
LEXICO: #T0 #supreme #judge #lucas #MCP

## T0 (Supreme Judge — Lucas)
```
T0 → OpenCode chat → MCP SSE :8766
   │
   ├── @CICLO2 → Pipeline completo
   ├── @AUDIT  → audit.full (18 seções)
   ├── @STEP0  → Ler SSOT + MULTILAYER
   └── hitl.approve → Aprovação humana (R76)
```

@UBL T1
LEXICO: #T1 #senior #agents #delegated #governance

## T1 (Senior Agents — Delegated)
```
T1 → governance tool → 46 actions
   │
   ├── kpi.report → health score
   ├── compliance.gaps → gap detection
   ├── bsc.report → Balanced Scorecard
   ├── submit → CICLO 2 pipeline
   └── rca.analyze → 5 Porquês
```

@UBL T2
LEXICO: #T2 #junior #agents #pulse #auto

## T2 (Junior Agents — Automated)
```
T2 → PulseScheduler (300s) → 14 checks automáticos
   │
   ├── compliance gaps → fix automático
   ├── KPI compute → health score
   ├── file scan → ruff + mypy
   ├── integrity scan → YAML + MDC + Secrets
   ├── bias detection → viés
   ├── handoff auto → registro
   ├── semantic health → lobes/catalog
   ├── kaizen log → micro-melhorias
   ├── ALCOA check → WAL integrity
   ├── auto-submit → pipeline trigger
   ├── semantic boot → ULQ→TAGS→PREP
   └── self-healing → MCP restart
```

@UBL T3
LEXICO: #T3 #observers #readonly #consultas

## T3 (Observers — Read Only)
```
T3 → consultas → rule.list, audit.replay, session.stats
   │
   └── Não pode: criar, modificar, deletar, aprovar
```

@UBL RuleInterdependency
LEXICO: #rule #interdependency #graph #R01 #R123

## Rule Interdependency Graph

```
R01 (Naming) ──────────────── R24 (SSOT) ───────────── R42 (3Ws)
    │                              │
R04 (Locks) ── R06 (Zones) ── R14 (Lobes) ── R15 (Role)
    │
R05 (Deletion) ── R144 (BashGuard) ── R12 (Handoff)
    │
R09 (STEP0) ── R21 (Claims) ── R41 (RCA) ── R43 (5Whys)
    │              │
    └── R146 (Watcher) ── R123 (Regression)
    
R51 (Fail-Fast) ── RUTH (Ruff) ── MYPY ── PYRIGHT
    │
R64 (CircuitBreaker) ── R65 (Bulkhead) ── R72 (Backpressure)
    │
R73 (Alignment) ── R75 (XAI) ── R76 (HITL)
    │
R112 (YAML) ── R113 (MDC) ── R114 (Secrets) ── R115 (DeadCode)
    │
R116 (Memory) ── R117 (Header) ── R120 (Boot)
```

@UBL EngineRuleTrigger
LEXICO: #engine #rule #trigger #map #hook

## Engine → Rule → Trigger Map

```
FR-154 (KPIs) ────── R56 ── C: PulseScheduler 300s
FR-155 (Resiliency) ─ R64-R72 ── H: Gateway + C: Pulse
FR-156 (AI Gov) ──── R73-R82 ── H: Gateway + C: Pulse
FR-158 (Integrity) ─ R112-R115 ─ C: PulseScheduler
FR-164 (Claims) ──── R21 ── H: Gateway every action
FR-165 (Router) ──── R118 ── H: Gateway + C: Pulse
FR-166 (Domains) ─── R118 ── C: PulseScheduler
FR-167 (Guardian) ── R119 ── C: PulseScheduler
FR-169 (Cross-Ref) ─ R121 ── S: audit.full
FR-170 (Boot) ────── R120 ── C: PulseScheduler
FR-171 (DueDil) ──── R122 ── H: Gateway
FR-172 (P1) ─────── R123 ── H: Gateway + C: Pulse
```

@UBL LiteLLMPicoClawHierarchy
LEXICO: #litellm #picoclaw #deepseek #hierarchy #llm

## LiteLLM ↔ PicoClaw ↔ DeepSeek Hierarchy

```
T0 (Lucas) → OpenCode → MCP SSE :8766
                │
        ┌───────┴────────┐
        │                 │
   LiteLLM :4000    PicoClaw :18790
   (router LLM)     (A2A server)
        │                 │
   DeepSeek V4      DeepSeek V4
   (flash/pro)      (flash/pro)
        │                 │
   Ollama :11434    Delegated tasks
   (Qwen local)     to T1/T2 agents
```

@UBL ComoAdicionar
LEXICO: #adicionar #nova #engine #regra #tool #lobe

## Como Adicionar Novos Itens

1. **Nova Engine** → FR-XXX → registrar em NAMING + MULTILAYER + ULQ + Catálogo
2. **Nova Regra** → RXXX → MULTILAYER + BOOT manifest + @tag no header
3. **Nova Tool** → NC-SUPER-XXX → Gateway wire + server.py registration
4. **Novo Lobe** → NC-LBE-FR-XXX-001.mdc → YAML header + _INDEX + Catálogo

@UBL BasePromptsCustomizados
LEXICO: #base #prompts #customizados #agente #T1

## Base para Prompts Customizados

```
Você é um agente T1 do NeoCortex. Sua função é executar ações delegadas.
Regras ativas: R01-R127. Ciclo atual: @CICLO2.
Antes de responder: @RAC @3W @KISS.
Se violar: STEP 0 → regression buffer → CentralWatcher.
Após cada ação: @SSOT (header 20 linhas).
```

*Este mapa é vivo — cada nova adição ao sistema DEVE ser registrada aqui.*
