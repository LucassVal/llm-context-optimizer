id: NC-DS-047
code: GLOBAL-SANE-001
status: DONE
title: Saneamento Global - Fase 1 (Core e Lobos)
assignee: Agente OpenCode na porta 34363
context:
- .agents/rules/NC-RULE-007-tagging-system.mdc
- 01_neocortex_framework/DIR-DOC-FR-001-docs-main/NC-DOC-FR-001-ubiquitous-language-dictionary.md
task: '**OBJETIVO**: Aplicar massivamente metadados de frente (Frontmatter) em todos
  os arquivos Markdown, Python e Json da zona Core (`01_neocortex_framework\neocortex`)
  e Lobos (`02_memory_lobes`).


  **INSTRUÇÕES DO PROMPT**:

  Você DEVE vasculhar de forma massiva com grep ou scripts os arquivos que NÃO possuem
  cabeçalho.

  Para cada arquivo, encontre o sentido dele através da "Linguagem Ubíqua" (ex: auth,
  database/ledger) no Dicionário Oficial.

  Injete no TOP do arquivo a estrutura de tags/dominio/layer e deixe `hash: auto-generated`.

  Valide os pythons depois de injetar os comments block ("""--- ... ---""") com `python
  -m py_compile`.

  '
protocol:
  rca_applied: true
  three_w_applied: true
  kiss_check: <2500 chars per ticket description
  validation:
    py:
    - ruff check --fix
    - ruff format
    - ruff check
    - mypy --ignore-missing-imports
    - py_compile
    - bandit -q
    yaml:
    - yaml.safe_load
    - naming NC- check
    - secret scan
    mdc:
    - frontmatter YAML validate
    - NC-READ-HASH check
    - naming NC- check
    json:
    - json.loads parse
    - naming NC- check
    all:
    - '@LOCKS check'
    - Write Zone check
    - '@SSOT update'
    - Handoff YAML
    - 3W check
    - SSOT Header
  sandbox_required: true
  sandbox_steps:
  - 1. Copiar arquivo alvo para *-sandbox.py
  - 2. Fazer alteracoes no sandbox
  - 3. Validar sandbox (ruff+mypy+py_compile)
  - 4. Testar sandbox
  - 5. Aplicar no real (com backup)
  - 6. Rollback via backup se falhar
  handoff_required: true
  handoff_template: DIR-DS-001-tickets/NC-DS-HANDOFF-TEMPLATE.yaml
  handoff_dest: DIR-DS-002-audit-logs/
  handoff_format: NC-DS-{TICKET_ID}-handoff-{YYYYMMDDTHHMMSS}.yaml
  handoff_fields:
  - ticket_id
  - status
  - submitted_at
  - submitted_by
  - summary
  - files_created
  - files_modified
  - validation_rounds
  - barriers_passed
  - locks_violated
  - t0_review
  governance:
    eisenhower_applied: true
    mordaca_h: HOOK — bloqueia acao invalida ANTES de executar (FAIL-FAST)
    mordaca_c: CHECKPOINT — audita estado a cada 300s (PulseScheduler)
    mordaca_s: SCHEDULE — consolida no CICLO 4 semanal
    mordaca_u: USER — T0 aprova/rejeita via handoff review
    kaizen: Registrar micro-melhoria no @CHANGELOG apos conclusao
    roadmap: Marcar %DONE no @ROADMAP apos handoff aprovado
