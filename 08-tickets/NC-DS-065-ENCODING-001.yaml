# @UBL @UBL @DS-065 | LEXICO: #TICKETS
id: NC-DS-065
code: ENCODING-001
status: DONE
title: Preparação de Ambiente com Encoding UTF-8 (Engineer)
assignee: Agente OpenCode (DeepSeek-Reasoner)
context:
- 01_neocortex_framework/scripts/NC-SCR-FR-062-engineer-encoding-fix.py
- DIR-DS-002-audit-logs/NC-DS-065-handoff-engineer-encoding-fix.yaml
task: '**OBJETIVO**: Preparar ambiente com encoding UTF‑8 para Windows. Verificar
  scripts Python e dependências para garantir compatibilidade com caracteres Unicode
  (emojis, acentos).


  **ANÁLISE REALIZADA**:

  1. Verificação de encoding UTF‑8 em todos os scripts Python

  2. Validação de dependências para testes VectorEngine

  3. Simulação de renomeação (dry run) para detecção de conflitos

  4. Validação STEP‑0 completa (Python 3.12.10, ruff, pytest)


  **RESULTADO**: Nenhuma modificação necessária — scripts já possuem encoding configurado
  corretamente.


  **IMPACTO**: Ambiente validado para execução de scripts com caracteres Unicode,
  prevenindo erros de encoding em pipelines de auditoria.


  **EVIDÊNCIAS**: Handoff correspondente em `DIR‑DS‑002‑audit‑logs/NC‑DS‑065‑handoff‑engineer‑encoding‑fix.yaml`.

  '
entry_state:
  files_exist:
  - 01_neocortex_framework/scripts/NC-SCR-FR-062-engineer-encoding-fix.py
  files_must_not_exist: []
exit_state:
  files_created: []
  files_modified: []
  files_deleted: []
  ssot_updated: false
  compiles_ok: true
  roadmap_done: null
barriers:
- id: B1
  check: naming_convention
  rule: Arquivo deve seguir padrão NC-
  block_if_fail: true
- id: B2
  check: lock_guard
  rule: 'NUNCA modificar: server.py, sub_server.py, NC-NAM-FR-001'
  block_if_fail: true
limits:
  max_validation_rounds: 3
  escalate_to_t0_after: 2
  max_tokens_per_phase: 2000
integrity_hash: 5a4b3c2d1e0f9a8b
protocol:
  rca_applied: true
  three_w_applied: true
  kiss_check: <2500 chars per ticket description
  validation:
    py:
    - ruff check --fix
    - ruff format
    - ruff check
    - mypy --ignore-missing-imports
    - py_compile
    - bandit -q
    yaml:
    - yaml.safe_load
    - naming NC- check
    - secret scan
    mdc:
    - frontmatter YAML validate
    - NC-READ-HASH check
    - naming NC- check
    json:
    - json.loads parse
    - naming NC- check
    all:
    - '@LOCKS check'
    - Write Zone check
    - '@SSOT update'
    - Handoff YAML
    - 3W check
    - SSOT Header
  sandbox_required: true
  sandbox_steps:
  - 1. Copiar arquivo alvo para *-sandbox.py
  - 2. Fazer alteracoes no sandbox
  - 3. Validar sandbox (ruff+mypy+py_compile)
  - 4. Testar sandbox
  - 5. Aplicar no real (com backup)
  - 6. Rollback via backup se falhar
  handoff_required: true
  handoff_template: DIR-DS-001-tickets/NC-DS-HANDOFF-TEMPLATE.yaml
  handoff_dest: DIR-DS-002-audit-logs/
  handoff_format: NC-DS-{TICKET_ID}-handoff-{YYYYMMDDTHHMMSS}.yaml
  handoff_fields:
  - ticket_id
  - status
  - submitted_at
  - submitted_by
  - summary
  - files_created
  - files_modified
  - validation_rounds
  - barriers_passed
  - locks_violated
  - t0_review
  governance:
    eisenhower_applied: true
    mordaca_h: HOOK — bloqueia acao invalida ANTES de executar (FAIL-FAST)
    mordaca_c: CHECKPOINT — audita estado a cada 300s (PulseScheduler)
    mordaca_s: SCHEDULE — consolida no CICLO 4 semanal
    mordaca_u: USER — T0 aprova/rejeita via handoff review
    kaizen: Registrar micro-melhoria no @CHANGELOG apos conclusao
    roadmap: Marcar %DONE no @ROADMAP apos handoff aprovado
